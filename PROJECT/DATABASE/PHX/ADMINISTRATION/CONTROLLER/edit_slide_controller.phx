// -- IMPORTS

import 'controller.php';
import '../../MODEL/slide_model.php';

// -- TYPES

class EDIT_SLIDE_CONTROLLER extends CONTROLLER
{
    method constructor(
        int slide_id
        )
    {
        parent::constructor();

        .Title = 'Edit a slide';
        .Slide = GetDatabaseSlideById( slide_id );

        import '../VIEW/edit_slide_view.php';
    }
}

// -- STATEMENTS

var edit_slide_controller = new EDIT_SLIDE_CONTROLLER( var slide_id );
