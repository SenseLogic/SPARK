// -- IMPORTS

import 'controller.php';
import '../../MODEL/category_model.php';

// -- TYPES

class EDIT_CATEGORY_CONTROLLER extends CONTROLLER
{
    method constructor(
        int category_id
        )
    {
        parent::constructor();

        .Title = 'Edit a category';
        .Category = GetDatabaseCategoryById( category_id );

        import '../VIEW/edit_category_view.php';
    }
}

// -- STATEMENTS

var edit_category_controller = new EDIT_CATEGORY_CONTROLLER( var category_id );
