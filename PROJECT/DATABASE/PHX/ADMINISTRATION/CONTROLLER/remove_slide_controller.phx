// -- IMPORTS

import 'controller.php';
import '../../MODEL/slide_model.php';

// -- TYPES

class REMOVE_SLIDE_CONTROLLER extends CONTROLLER
{
    method constructor(
        int slide_id
        )
    {
        parent::constructor();

        .Title = 'Remove a slide';
        .Slide = GetDatabaseSlideById( slide_id );

        import '../VIEW/remove_slide_view.php';
    }
}

// -- STATEMENTS

var remove_slide_controller = new REMOVE_SLIDE_CONTROLLER( var slide_id );
