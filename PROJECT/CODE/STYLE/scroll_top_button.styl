.scroll-top-button
{
    z-index: 999;
    position: fixed;
    bottom: 10px;
    right: 10px;

    overflow: visible;
    height: 50px;
    width: 20px;

    opacity: 1;
    background: url( "/static/image/common/scroll_top_icon.svg" ) no-repeat center center;
    background-size: contain;

    cursor: pointer;
    transition: opacity 0.3s ease;
}

body:not( .is-scrolled ) .scroll-top-button
{
    opacity: 0;

    pointer-events: none;
}
