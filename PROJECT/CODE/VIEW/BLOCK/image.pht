<style file="image.styl">
    .image
    {
        max-height: var( --viewport-height );
        width: 100%;
        padding: 1rem;

        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: start;

        color: WhiteColor;

        +Media( min-width-65em )
        {
            padding: 2rem;
        }
    }
</style>
<div id="<# var block.Slug #>" class="y-translation-opacity-transition block appearing-block image" style="padding-top: <# block.MinimumHeight #>; background: url( '<# block.ImagePath #>' ) no-repeat <# block.ImageHorizontalPosition #> <# block.ImageVerticalPosition #> / cover, url( '<# GetPreloadImagePath( block.ImagePath ) #>' ) no-repeat <# block.ImageHorizontalPosition #> <# block.ImageVerticalPosition #> / cover">
    <? include str_replace( '-', '_', block.ContentSlug ) .. '_content.php'; ?>
</div>
