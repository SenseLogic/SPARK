// -- IMPORTS

import 'controller.php';
import '../../MODEL/article_model.php';

// -- TYPES

class DO_ADD_ARTICLE_CONTROLLER extends CONTROLLER
{
    // -- CONSTRUCTORS

    method constructor(
        string language_code
        )
    {
        parent::constructor( language_code );

        var slug = GetPostValue( 'Slug' );
        var name = GetPostValue( 'Name' );
        var text = GetPostValue( 'Text' );
        var image_path = GetPostValue( 'ImagePath' );
        var video_path = GetPostValue( 'VideoPath' );
        var next_article_id = GetPostValue( 'NextArticleId' );
        var block_slug_array = GetJsonObject( GetPostValue( 'BlockSlugArray' ) );
        var priority = GetPostValue( 'Priority' );
        var is_active = GetPostValue( 'IsActive' );

        AddDatabaseArticle( slug, name, text, image_path, video_path, next_article_id, block_slug_array, priority, is_active );

        Redirect( FindSessionValue( 'ListPage', '/admin/article' ) );
    }
}

// -- STATEMENTS

var do_add_article_controller = new DO_ADD_ARTICLE_CONTROLLER( var language_code );
