// -- IMPORTS

import 'controller.php';
import '../../MODEL/article_model.php';

// -- TYPES

class EDIT_ARTICLE_CONTROLLER extends CONTROLLER
{
    // -- CONSTRUCTORS

    method constructor(
        string language_code,
        article_id
        )
    {
        parent::constructor( language_code );

        .Title = 'Edit a article';
        .Article = GetDatabaseArticleById( article_id );
        .ListPage = FindSessionValue( 'ListPage', '/admin/article' );

        import '../VIEW/edit_article_view.php';
    }
}

// -- STATEMENTS

var edit_article_controller = new EDIT_ARTICLE_CONTROLLER( var language_code, var article_id );
