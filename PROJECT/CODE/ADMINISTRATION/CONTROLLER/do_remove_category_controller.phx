// -- IMPORTS

import 'controller.php';
import '../../MODEL/category_model.php';

// -- TYPES

class DO_REMOVE_CATEGORY_CONTROLLER extends CONTROLLER
{
    method constructor(
        int category_id
        )
    {
        parent::constructor();

        RemoveDatabaseCategoryById( category_id );

        Redirect( '/admin/category' );
    }
}

// -- STATEMENTS

var do_remove_category_controller = new DO_REMOVE_CATEGORY_CONTROLLER( var category_id );
